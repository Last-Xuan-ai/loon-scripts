# 字母圈播放修复

适用于 **Loon 3.5.1（983）或更新版本**，按 2026-09-24 查看到的官方 Script v2 文档编写。

## 在 Loon 中订阅（推荐）

1. 复制插件地址：

   `https://raw.githubusercontent.com/Last-Xuan-ai/loon-scripts/main/zmq.lpx`

2. 打开 Loon，进入“插件”，点击添加按钮，粘贴上述 URL 并保存。如果页面先显示“配置”，从配置页进入插件管理。
3. 停用旧版字母圈脚本，开启本插件、脚本功能和 MitM。HTTPS 响应修改要求 Loon 的 MitM 证书已安装并受信任。
4. 重新打开播放页。成功后，播放区会出现视频控件，以及“重新加载”“打开视频地址”。

插件中的脚本地址已经指向同一公开仓库，会由 Loon 下载，无需额外手工导入 JS。以后在 Loon 更新插件及脚本缓存即可获取仓库的新版本。

仓库：[Last-Xuan-ai/loon-scripts](https://github.com/Last-Xuan-ai/loon-scripts)。用户已选择公开发布，以上 Raw 下载链接无需 GitHub 登录或访问令牌。

## 离线安装包

从仓库 Releases 下载 `zmq-loon-fixed.zip` 并解压。将 `zmq.js` 加入 Loon 的本地脚本资源，再把 `zmq.lpx` 中 `script(...)` 的远程地址改为 `script("zmq.js")`（或实际本地路径），导入修改后的插件。离线方式需要手工替换文件以更新。

最初提供的 `chmg2025/script/.../zmq.js` 只有注释中的配置，可执行代码实际在 `chmg2025/js/.../zmq.js`。本仓库将配置和代码分为 `.lpx`、`.js` 两个文件，与原作者仓库分开维护。

## 覆盖范围

脚本正则匹配 `zimuquan`、`zmqurl`、`zmqsite` 后接任意数字的 `.top`、`.com`、`.uk` 域名，并接受 `www`、`www1`、`www6` 等子域名。MitM 使用官方支持的 `*` 通配符，同时覆盖根域名和子域名。

发布页当前列出的站点地址全部通过规则匹配检查。未来仅改变编号或子域名时无需补充；若更换品牌名称或顶级域名，需要同步更新脚本、插件中的 URL 正则及 MitM 列表。

只处理 `/index.php/vod/play/id/.../sid/.../nid/....html`，也接受省略 `/index.php` 的同类路径和查询参数。发布页首页无需改写，它提供的播放站点才是修复对象。

## 修复内容

- 保留 `decodeURIComponent(atob(...))` 的编码方式，避免中文乱码和百分号导致解码异常。
- 优先使用页面已有的真实视频地址；否则从当前播放项的 `.../vod.jpg` 等封面推导同目录 `index.m3u8`，保留完整路径、端口和查询参数，兼容 `/video/`、`/sp/` 等存储目录。
- 不使用推荐列表的封面。只替换播放器区域，保留手机页返回按钮和页面其他内容。
- Safari 优先使用原生 HLS；其他浏览器加载站点 HLS 库，失败后尝试固定版本的公共库。提供重试和视频地址入口，不强制自动播放。
- 未识别的模板或解析异常使用 `$done({})` 放行原响应，每次仅调用一次 `$done`。

## 验证与边界

通过 21 项自动检查，覆盖真实电脑/手机页面回放、编码、地址与查询参数、域名覆盖、已有播放器地址、原生 HLS 与库加载分支、重复处理、异常放行，以及官方转换器对脚本调用及选项的核对。

用户提供的 `id/205539` 示例，其 HLS 清单请求成功，ffprobe 实际读取并解析出 1280×720 H.264 视频流和 AAC 音频流。这验证了样本清单及所读取的媒体数据，不等于逐段播放整部视频。

尚未在用户设备的 Loon 内导入运行，也没有逐个播放所有站点的全部视频。源站资源删除、过期链接、鉴权、网络阻断或新模板无法靠域名正则保证修复。如果播放器已替换但加载失败，可通过“打开视频地址”区分源站/网络问题与播放器问题。

## 官方新标准与排查

- [Rewrite v2](https://nsloon.app/docs/Rewrite/rewrite_v2)：从 3.5.1（978）起支持。
- [Script v2](https://nsloon.app/docs/Script/script_v2)：从 3.5.1（983）起支持，使用 `response if ... then script(...) with ...`，选项写作 `requires_body`。
- [Script API](https://nsloon.app/docs/Script/script_api)：`$done({})` 放行，修改 Body 后由 Loon 处理相关传输头。
- [MitM](https://nsloon.app/docs/MitM/)：hostname 支持 `*`、`?` 通配符。

本插件使用 `[Script]`，无需另加 Body Rewrite。官方规定：命中 Response Body Rewrite 会阻止 Response Script；同一响应只选择第一条匹配的 Response Script。因此旧字母圈脚本或其他覆盖同一页面的响应改写可能冲突。

没有出现新播放器时，检查 Loon 版本是否达到 983、脚本路径是否有效、MitM 是否生效、旧脚本是否抢先命中，以及脚本日志中的 `[ZMQ]` 信息。
